/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.selfhosted.v1.distributioncredentials;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.DeepgramApiApiException;
import com.deepgram.core.DeepgramApiException;
import com.deepgram.core.DeepgramApiHttpResponse;
import com.deepgram.core.MediaTypes;
import com.deepgram.core.ObjectMappers;
import com.deepgram.core.QueryStringMapper;
import com.deepgram.core.RequestOptions;
import com.deepgram.errors.BadRequestError;
import com.deepgram.resources.selfhosted.v1.distributioncredentials.requests.CreateProjectDistributionCredentialsV1Request;
import com.deepgram.types.CreateProjectDistributionCredentialsV1Response;
import com.deepgram.types.GetProjectDistributionCredentialsV1Response;
import com.deepgram.types.ListProjectDistributionCredentialsV1Response;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawDistributionCredentialsClient {
    protected final ClientOptions clientOptions;

    public RawDistributionCredentialsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists sets of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<ListProjectDistributionCredentialsV1Response> list(String projectId) {
        return list(projectId, null);
    }

    /**
     * Lists sets of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<ListProjectDistributionCredentialsV1Response> list(
            String projectId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("self-hosted/distribution")
                .addPathSegments("credentials");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListProjectDistributionCredentialsV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<CreateProjectDistributionCredentialsV1Response> create(String projectId) {
        return create(
                projectId,
                CreateProjectDistributionCredentialsV1Request.builder().build());
    }

    /**
     * Creates a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<CreateProjectDistributionCredentialsV1Response> create(
            String projectId, RequestOptions requestOptions) {
        return create(
                projectId,
                CreateProjectDistributionCredentialsV1Request.builder().build(),
                requestOptions);
    }

    /**
     * Creates a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<CreateProjectDistributionCredentialsV1Response> create(
            String projectId, CreateProjectDistributionCredentialsV1Request request) {
        return create(projectId, request, null);
    }

    /**
     * Creates a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<CreateProjectDistributionCredentialsV1Response> create(
            String projectId, CreateProjectDistributionCredentialsV1Request request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("self-hosted/distribution")
                .addPathSegments("credentials");
        if (request.getProvider().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider", request.getProvider().get(), false);
        }
        if (request.getScopes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "scopes", request.getScopes().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, CreateProjectDistributionCredentialsV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<GetProjectDistributionCredentialsV1Response> get(
            String projectId, String distributionCredentialsId) {
        return get(projectId, distributionCredentialsId, null);
    }

    /**
     * Returns a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<GetProjectDistributionCredentialsV1Response> get(
            String projectId, String distributionCredentialsId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("self-hosted/distribution/credentials")
                .addPathSegment(distributionCredentialsId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, GetProjectDistributionCredentialsV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<GetProjectDistributionCredentialsV1Response> delete(
            String projectId, String distributionCredentialsId) {
        return delete(projectId, distributionCredentialsId, null);
    }

    /**
     * Deletes a set of distribution credentials for the specified project
     */
    public DeepgramApiHttpResponse<GetProjectDistributionCredentialsV1Response> delete(
            String projectId, String distributionCredentialsId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("self-hosted/distribution/credentials")
                .addPathSegment(distributionCredentialsId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, GetProjectDistributionCredentialsV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }
}
