/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.listen.v1.media;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.DeepgramApiException;
import com.deepgram.core.DeepgramApiHttpResponse;
import com.deepgram.core.DeepgramHttpException;
import com.deepgram.core.InputStreamRequestBody;
import com.deepgram.core.MediaTypes;
import com.deepgram.core.ObjectMappers;
import com.deepgram.core.QueryStringMapper;
import com.deepgram.core.RequestOptions;
import com.deepgram.errors.BadRequestError;
import com.deepgram.resources.listen.v1.media.requests.ListenV1RequestUrl;
import com.deepgram.resources.listen.v1.media.requests.MediaTranscribeRequestOctetStream;
import com.deepgram.resources.listen.v1.media.types.MediaTranscribeResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawMediaClient {
    protected final ClientOptions clientOptions;

    public AsyncRawMediaClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeUrl(
            ListenV1RequestUrl request) {
        return transcribeUrl(request, null);
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeUrl(
            ListenV1RequestUrl request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/listen");
        if (request.getCallback().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback", request.getCallback().get(), false);
        }
        if (request.getCallbackMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback_method", request.getCallbackMethod().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "sentiment", request.getSentiment().orElse(false), false);
        if (request.getSummarize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "summarize", request.getSummarize().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "topics", request.getTopics().orElse(false), false);
        if (request.getCustomTopicMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic_mode", request.getCustomTopicMode().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "intents", request.getIntents().orElse(false), false);
        if (request.getCustomIntentMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent_mode", request.getCustomIntentMode().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "detect_entities", request.getDetectEntities().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "detect_language", request.getDetectLanguage().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "diarize", request.getDiarize().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "dictation", request.getDictation().orElse(false), false);
        if (request.getEncoding().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "encoding", request.getEncoding().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "filler_words", request.getFillerWords().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "language", request.getLanguage().orElse("en"), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "measurements", request.getMeasurements().orElse(false), false);
        if (request.getModel().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "model", request.getModel().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "multichannel", request.getMultichannel().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "numerals", request.getNumerals().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "paragraphs", request.getParagraphs().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "profanity_filter", request.getProfanityFilter().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "punctuate", request.getPunctuate().orElse(false), false);
        if (request.getRedact().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "redact", request.getRedact().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "smart_format", request.getSmartFormat().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "utterances", request.getUtterances().orElse(false), false);
        if (request.getUttSplit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "utt_split", request.getUttSplit().get(), false);
        }
        if (request.getVersion().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "version", request.getVersion().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "mip_opt_out", request.getMipOptOut().orElse(false), false);
        if (request.getExtra().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "extra", request.getExtra().get(), true);
        }
        if (request.getTag().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "tag", request.getTag().get(), true);
        }
        if (request.getCustomTopic().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic", request.getCustomTopic().get(), true);
        }
        if (request.getCustomIntent().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent", request.getCustomIntent().get(), true);
        }
        if (request.getKeyterm().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "keyterm", request.getKeyterm().get(), true);
        }
        if (request.getKeywords().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "keywords", request.getKeywords().get(), true);
        }
        if (request.getReplace().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "replace", request.getReplace().get(), true);
        }
        if (request.getSearch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "search", request.getSearch().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new DeepgramApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MediaTranscribeResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 400) {
                            future.completeExceptionally(new BadRequestError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new DeepgramHttpException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeFile(byte[] body) {
        return transcribeFile(
                MediaTranscribeRequestOctetStream.builder().body(body).build());
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeFile(
            byte[] body, RequestOptions requestOptions) {
        return transcribeFile(
                MediaTranscribeRequestOctetStream.builder().body(body).build(), requestOptions);
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeFile(
            MediaTranscribeRequestOctetStream request) {
        return transcribeFile(request, null);
    }

    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     */
    public CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> transcribeFile(
            MediaTranscribeRequestOctetStream request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/listen");
        if (request.getCallback().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback", request.getCallback().get(), false);
        }
        if (request.getCallbackMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback_method", request.getCallbackMethod().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "sentiment", request.getSentiment().orElse(false), false);
        if (request.getSummarize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "summarize", request.getSummarize().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "topics", request.getTopics().orElse(false), false);
        if (request.getCustomTopicMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic_mode", request.getCustomTopicMode().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "intents", request.getIntents().orElse(false), false);
        if (request.getCustomIntentMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent_mode", request.getCustomIntentMode().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "detect_entities", request.getDetectEntities().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "detect_language", request.getDetectLanguage().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "diarize", request.getDiarize().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "dictation", request.getDictation().orElse(false), false);
        if (request.getEncoding().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "encoding", request.getEncoding().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "filler_words", request.getFillerWords().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "language", request.getLanguage().orElse("en"), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "measurements", request.getMeasurements().orElse(false), false);
        if (request.getModel().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "model", request.getModel().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "multichannel", request.getMultichannel().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "numerals", request.getNumerals().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "paragraphs", request.getParagraphs().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "profanity_filter", request.getProfanityFilter().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "punctuate", request.getPunctuate().orElse(false), false);
        if (request.getRedact().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "redact", request.getRedact().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "smart_format", request.getSmartFormat().orElse(false), false);
        QueryStringMapper.addQueryParameter(
                httpUrl, "utterances", request.getUtterances().orElse(false), false);
        if (request.getUttSplit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "utt_split", request.getUttSplit().get(), false);
        }
        if (request.getVersion().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "version", request.getVersion().get(), false);
        }
        QueryStringMapper.addQueryParameter(
                httpUrl, "mip_opt_out", request.getMipOptOut().orElse(false), false);
        if (request.getExtra().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "extra", request.getExtra().get(), true);
        }
        if (request.getTag().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "tag", request.getTag().get(), true);
        }
        if (request.getCustomTopic().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic", request.getCustomTopic().get(), true);
        }
        if (request.getCustomIntent().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent", request.getCustomIntent().get(), true);
        }
        if (request.getKeyterm().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "keyterm", request.getKeyterm().get(), true);
        }
        if (request.getKeywords().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "keywords", request.getKeywords().get(), true);
        }
        if (request.getReplace().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "replace", request.getReplace().get(), true);
        }
        if (request.getSearch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "search", request.getSearch().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body = new InputStreamRequestBody(
                MediaType.parse("application/octet-stream"), new ByteArrayInputStream(request.getBody()));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/octet-stream")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<DeepgramApiHttpResponse<MediaTranscribeResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new DeepgramApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MediaTranscribeResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 400) {
                            future.completeExceptionally(new BadRequestError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new DeepgramHttpException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
