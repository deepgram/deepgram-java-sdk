/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.speak.v2.types;

import com.deepgram.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SpeakV2SpeechInterrupted.Builder.class)
public final class SpeakV2SpeechInterrupted {
    private final int audioPlayedMs;

    private final Optional<String> textSpoken;

    private final Optional<String> textRemaining;

    private final SpeakV2SpeechInterruptedMetadata metadata;

    private final Map<String, Object> additionalProperties;

    private SpeakV2SpeechInterrupted(
            int audioPlayedMs,
            Optional<String> textSpoken,
            Optional<String> textRemaining,
            SpeakV2SpeechInterruptedMetadata metadata,
            Map<String, Object> additionalProperties) {
        this.audioPlayedMs = audioPlayedMs;
        this.textSpoken = textSpoken;
        this.textRemaining = textRemaining;
        this.metadata = metadata;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Message type identifier
     */
    @JsonProperty("type")
    public String getType() {
        return "SpeechInterrupted";
    }

    /**
     * @return How much audio the client had played when the interrupt landed, in milliseconds from the start of the session. Echoes the <code>Interrupt</code>'s <code>playback_offset</code> when one was supplied. Otherwise it is the server's own total, representing the audio that has been generated so far. A client that sends its first <code>Interrupt</code> without an offset can use this value as the baseline the next one must advance past.
     */
    @JsonProperty("audio_played_ms")
    public int getAudioPlayedMs() {
        return audioPlayedMs;
    }

    /**
     * @return The portion of the turn's text the user heard. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.
     */
    @JsonProperty("text_spoken")
    public Optional<String> getTextSpoken() {
        return textSpoken;
    }

    /**
     * @return The portion of the turn's text the user did not hear. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.
     */
    @JsonProperty("text_remaining")
    public Optional<String> getTextRemaining() {
        return textRemaining;
    }

    /**
     * @return Billing and timing for a single turn.
     */
    @JsonProperty("metadata")
    public SpeakV2SpeechInterruptedMetadata getMetadata() {
        return metadata;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SpeakV2SpeechInterrupted && equalTo((SpeakV2SpeechInterrupted) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SpeakV2SpeechInterrupted other) {
        return audioPlayedMs == other.audioPlayedMs
                && textSpoken.equals(other.textSpoken)
                && textRemaining.equals(other.textRemaining)
                && metadata.equals(other.metadata);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.audioPlayedMs, this.textSpoken, this.textRemaining, this.metadata);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AudioPlayedMsStage builder() {
        return new Builder();
    }

    public interface AudioPlayedMsStage {
        /**
         * <p>How much audio the client had played when the interrupt landed, in milliseconds from the start of the session. Echoes the <code>Interrupt</code>'s <code>playback_offset</code> when one was supplied. Otherwise it is the server's own total, representing the audio that has been generated so far. A client that sends its first <code>Interrupt</code> without an offset can use this value as the baseline the next one must advance past.</p>
         */
        MetadataStage audioPlayedMs(int audioPlayedMs);

        Builder from(SpeakV2SpeechInterrupted other);
    }

    public interface MetadataStage {
        /**
         * <p>Billing and timing for a single turn.</p>
         */
        _FinalStage metadata(@NotNull SpeakV2SpeechInterruptedMetadata metadata);
    }

    public interface _FinalStage {
        SpeakV2SpeechInterrupted build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The portion of the turn's text the user heard. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         */
        _FinalStage textSpoken(Optional<String> textSpoken);

        _FinalStage textSpoken(String textSpoken);

        /**
         * <p>The portion of the turn's text the user did not hear. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         */
        _FinalStage textRemaining(Optional<String> textRemaining);

        _FinalStage textRemaining(String textRemaining);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AudioPlayedMsStage, MetadataStage, _FinalStage {
        private int audioPlayedMs;

        private SpeakV2SpeechInterruptedMetadata metadata;

        private Optional<String> textRemaining = Optional.empty();

        private Optional<String> textSpoken = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SpeakV2SpeechInterrupted other) {
            audioPlayedMs(other.getAudioPlayedMs());
            textSpoken(other.getTextSpoken());
            textRemaining(other.getTextRemaining());
            metadata(other.getMetadata());
            return this;
        }

        /**
         * <p>How much audio the client had played when the interrupt landed, in milliseconds from the start of the session. Echoes the <code>Interrupt</code>'s <code>playback_offset</code> when one was supplied. Otherwise it is the server's own total, representing the audio that has been generated so far. A client that sends its first <code>Interrupt</code> without an offset can use this value as the baseline the next one must advance past.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("audio_played_ms")
        public MetadataStage audioPlayedMs(int audioPlayedMs) {
            this.audioPlayedMs = audioPlayedMs;
            return this;
        }

        /**
         * <p>Billing and timing for a single turn.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("metadata")
        public _FinalStage metadata(@NotNull SpeakV2SpeechInterruptedMetadata metadata) {
            this.metadata = Objects.requireNonNull(metadata, "metadata must not be null");
            return this;
        }

        /**
         * <p>The portion of the turn's text the user did not hear. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage textRemaining(String textRemaining) {
            this.textRemaining = Optional.ofNullable(textRemaining);
            return this;
        }

        /**
         * <p>The portion of the turn's text the user did not hear. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "text_remaining", nulls = Nulls.SKIP)
        public _FinalStage textRemaining(Optional<String> textRemaining) {
            this.textRemaining = textRemaining;
            return this;
        }

        /**
         * <p>The portion of the turn's text the user heard. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage textSpoken(String textSpoken) {
            this.textSpoken = Optional.ofNullable(textSpoken);
            return this;
        }

        /**
         * <p>The portion of the turn's text the user heard. Omitted when the <code>Interrupt</code> carried no <code>playback_offset</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "text_spoken", nulls = Nulls.SKIP)
        public _FinalStage textSpoken(Optional<String> textSpoken) {
            this.textSpoken = textSpoken;
            return this;
        }

        @java.lang.Override
        public SpeakV2SpeechInterrupted build() {
            return new SpeakV2SpeechInterrupted(
                    audioPlayedMs, textSpoken, textRemaining, metadata, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
