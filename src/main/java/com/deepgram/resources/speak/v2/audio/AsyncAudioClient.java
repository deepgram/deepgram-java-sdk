/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.speak.v2.audio;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.RequestOptions;
import com.deepgram.resources.speak.v2.audio.requests.SpeakV2Request;
import java.io.InputStream;
import java.util.concurrent.CompletableFuture;

public class AsyncAudioClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawAudioClient rawClient;

    public AsyncAudioClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawAudioClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawAudioClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Synthesize a complete block of text into a single audio response using Deepgram's Flux TTS batch (REST) API. Use this for pre-rendering fixed audio (IVR prompts, notifications, narration) where the whole text is known up front and you don't need incremental playback or interruption.
     */
    public CompletableFuture<InputStream> generate(SpeakV2Request request) {
        return this.rawClient.generate(request).thenApply(response -> response.body());
    }

    /**
     * Synthesize a complete block of text into a single audio response using Deepgram's Flux TTS batch (REST) API. Use this for pre-rendering fixed audio (IVR prompts, notifications, narration) where the whole text is known up front and you don't need incremental playback or interruption.
     */
    public CompletableFuture<InputStream> generate(SpeakV2Request request, RequestOptions requestOptions) {
        return this.rawClient.generate(request, requestOptions).thenApply(response -> response.body());
    }
}
