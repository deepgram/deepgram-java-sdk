/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.manage.v1.projects.requests;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.DeepgramApiException;
import com.deepgram.core.DeepgramApiHttpResponse;
import com.deepgram.core.ObjectMappers;
import com.deepgram.core.QueryStringMapper;
import com.deepgram.core.RequestOptions;
import com.deepgram.errors.BadRequestError;
import com.deepgram.resources.manage.v1.projects.requests.requests.RequestsListRequest;
import com.deepgram.types.GetProjectRequestV1Response;
import com.deepgram.types.ListProjectRequestsV1Response;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawRequestsClient {
    protected final ClientOptions clientOptions;

    public RawRequestsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Generates a list of requests for a specific project
     */
    public DeepgramApiHttpResponse<ListProjectRequestsV1Response> list(String projectId) {
        return list(projectId, RequestsListRequest.builder().build());
    }

    /**
     * Generates a list of requests for a specific project
     */
    public DeepgramApiHttpResponse<ListProjectRequestsV1Response> list(
            String projectId, RequestOptions requestOptions) {
        return list(projectId, RequestsListRequest.builder().build(), requestOptions);
    }

    /**
     * Generates a list of requests for a specific project
     */
    public DeepgramApiHttpResponse<ListProjectRequestsV1Response> list(String projectId, RequestsListRequest request) {
        return list(projectId, request, null);
    }

    /**
     * Generates a list of requests for a specific project
     */
    public DeepgramApiHttpResponse<ListProjectRequestsV1Response> list(
            String projectId, RequestsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("requests");
        if (request.getStart().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "start", request.getStart().get(), false);
        }
        if (request.getEnd().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "end", request.getEnd().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getAccessor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "accessor", request.getAccessor().get(), false);
        }
        if (request.getRequestId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "request_id", request.getRequestId().get(), false);
        }
        if (request.getDeployment().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "deployment", request.getDeployment().get(), false);
        }
        if (request.getEndpoint().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "endpoint", request.getEndpoint().get(), false);
        }
        if (request.getMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "method", request.getMethod().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListProjectRequestsV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves a specific request for a specific project
     */
    public DeepgramApiHttpResponse<GetProjectRequestV1Response> get(String projectId, String requestId) {
        return get(projectId, requestId, null);
    }

    /**
     * Retrieves a specific request for a specific project
     */
    public DeepgramApiHttpResponse<GetProjectRequestV1Response> get(
            String projectId, String requestId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("requests")
                .addPathSegment(requestId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetProjectRequestV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }
}
