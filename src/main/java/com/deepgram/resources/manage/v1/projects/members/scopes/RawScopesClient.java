/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.manage.v1.projects.members.scopes;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.DeepgramApiException;
import com.deepgram.core.DeepgramApiHttpResponse;
import com.deepgram.core.DeepgramHttpException;
import com.deepgram.core.MediaTypes;
import com.deepgram.core.ObjectMappers;
import com.deepgram.core.RequestOptions;
import com.deepgram.errors.BadRequestError;
import com.deepgram.resources.manage.v1.projects.members.scopes.requests.UpdateProjectMemberScopesV1Request;
import com.deepgram.types.ListProjectMemberScopesV1Response;
import com.deepgram.types.UpdateProjectMemberScopesV1Response;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawScopesClient {
    protected final ClientOptions clientOptions;

    public RawScopesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Retrieves a list of scopes for a specific member
     */
    public DeepgramApiHttpResponse<ListProjectMemberScopesV1Response> list(String projectId, String memberId) {
        return list(projectId, memberId, null);
    }

    /**
     * Retrieves a list of scopes for a specific member
     */
    public DeepgramApiHttpResponse<ListProjectMemberScopesV1Response> list(
            String projectId, String memberId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("members")
                .addPathSegment(memberId)
                .addPathSegments("scopes");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListProjectMemberScopesV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramHttpException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates the scopes for a specific member
     */
    public DeepgramApiHttpResponse<UpdateProjectMemberScopesV1Response> update(
            String projectId, String memberId, UpdateProjectMemberScopesV1Request request) {
        return update(projectId, memberId, request, null);
    }

    /**
     * Updates the scopes for a specific member
     */
    public DeepgramApiHttpResponse<UpdateProjectMemberScopesV1Response> update(
            String projectId,
            String memberId,
            UpdateProjectMemberScopesV1Request request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("members")
                .addPathSegment(memberId)
                .addPathSegments("scopes");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DeepgramApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, UpdateProjectMemberScopesV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramHttpException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }
}
