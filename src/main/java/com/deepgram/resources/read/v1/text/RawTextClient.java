/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.resources.read.v1.text;

import com.deepgram.core.ClientOptions;
import com.deepgram.core.DeepgramApiApiException;
import com.deepgram.core.DeepgramApiException;
import com.deepgram.core.DeepgramApiHttpResponse;
import com.deepgram.core.MediaTypes;
import com.deepgram.core.ObjectMappers;
import com.deepgram.core.QueryStringMapper;
import com.deepgram.core.RequestOptions;
import com.deepgram.errors.BadRequestError;
import com.deepgram.resources.read.v1.text.requests.TextAnalyzeRequest;
import com.deepgram.types.ReadV1Request;
import com.deepgram.types.ReadV1Response;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawTextClient {
    protected final ClientOptions clientOptions;

    public RawTextClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Analyze text content using Deepgrams text analysis API
     */
    public DeepgramApiHttpResponse<ReadV1Response> analyze(ReadV1Request body) {
        return analyze(TextAnalyzeRequest.builder().body(body).build());
    }

    /**
     * Analyze text content using Deepgrams text analysis API
     */
    public DeepgramApiHttpResponse<ReadV1Response> analyze(ReadV1Request body, RequestOptions requestOptions) {
        return analyze(TextAnalyzeRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Analyze text content using Deepgrams text analysis API
     */
    public DeepgramApiHttpResponse<ReadV1Response> analyze(TextAnalyzeRequest request) {
        return analyze(request, null);
    }

    /**
     * Analyze text content using Deepgrams text analysis API
     */
    public DeepgramApiHttpResponse<ReadV1Response> analyze(TextAnalyzeRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/read");
        if (request.getCallback().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback", request.getCallback().get(), false);
        }
        if (request.getCallbackMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "callback_method", request.getCallbackMethod().get(), false);
        }
        if (request.getSentiment().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sentiment", request.getSentiment().get(), false);
        }
        if (request.getSummarize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "summarize", request.getSummarize().get(), false);
        }
        if (request.getTopics().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "topics", request.getTopics().get(), false);
        }
        if (request.getCustomTopicMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic_mode", request.getCustomTopicMode().get(), false);
        }
        if (request.getIntents().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "intents", request.getIntents().get(), false);
        }
        if (request.getCustomIntentMode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent_mode", request.getCustomIntentMode().get(), false);
        }
        if (request.getLanguage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "language", request.getLanguage().get(), false);
        }
        if (request.getTag().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "tag", request.getTag().get(), true);
        }
        if (request.getCustomTopic().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_topic", request.getCustomTopic().get(), true);
        }
        if (request.getCustomIntent().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_intent", request.getCustomIntent().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ReadV1Response.class), response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }
}
