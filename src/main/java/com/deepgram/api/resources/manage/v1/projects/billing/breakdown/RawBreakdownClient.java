/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.api.resources.manage.v1.projects.billing.breakdown;

import com.deepgram.api.core.ClientOptions;
import com.deepgram.api.core.DeepgramApiApiException;
import com.deepgram.api.core.DeepgramApiException;
import com.deepgram.api.core.DeepgramApiHttpResponse;
import com.deepgram.api.core.ObjectMappers;
import com.deepgram.api.core.QueryStringMapper;
import com.deepgram.api.core.RequestOptions;
import com.deepgram.api.errors.BadRequestError;
import com.deepgram.api.resources.manage.v1.projects.billing.breakdown.requests.BreakdownListRequest;
import com.deepgram.api.types.BillingBreakdownV1Response;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawBreakdownClient {
    protected final ClientOptions clientOptions;

    public RawBreakdownClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Retrieves the billing summary for a specific project, with various filter options or by grouping options.
     */
    public DeepgramApiHttpResponse<BillingBreakdownV1Response> list(String projectId) {
        return list(projectId, BreakdownListRequest.builder().build());
    }

    /**
     * Retrieves the billing summary for a specific project, with various filter options or by grouping options.
     */
    public DeepgramApiHttpResponse<BillingBreakdownV1Response> list(String projectId, BreakdownListRequest request) {
        return list(projectId, request, null);
    }

    /**
     * Retrieves the billing summary for a specific project, with various filter options or by grouping options.
     */
    public DeepgramApiHttpResponse<BillingBreakdownV1Response> list(
            String projectId, BreakdownListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL())
                .newBuilder()
                .addPathSegments("v1/projects")
                .addPathSegment(projectId)
                .addPathSegments("billing")
                .addPathSegments("breakdown");
        if (request.getStart().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "start", request.getStart().get(), false);
        }
        if (request.getEnd().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "end", request.getEnd().get(), false);
        }
        if (request.getAccessor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "accessor", request.getAccessor().get(), false);
        }
        if (request.getDeployment().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "deployment", request.getDeployment().get(), false);
        }
        if (request.getTag().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "tag", request.getTag().get(), false);
        }
        if (request.getLineItem().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "line_item", request.getLineItem().get(), false);
        }
        if (request.getGrouping().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "grouping", request.getGrouping().get(), true);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DeepgramApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BillingBreakdownV1Response.class),
                        response);
            }
            try {
                if (response.code() == 400) {
                    throw new BadRequestError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DeepgramApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new DeepgramApiException("Network error executing HTTP request", e);
        }
    }
}
