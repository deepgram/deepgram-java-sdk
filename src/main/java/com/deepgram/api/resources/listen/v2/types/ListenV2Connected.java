/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.deepgram.api.resources.listen.v2.types;

import com.deepgram.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ListenV2Connected.Builder.class)
public final class ListenV2Connected {
    private final String requestId;

    private final double sequenceId;

    private final Map<String, Object> additionalProperties;

    private ListenV2Connected(String requestId, double sequenceId, Map<String, Object> additionalProperties) {
        this.requestId = requestId;
        this.sequenceId = sequenceId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Message type identifier
     */
    @JsonProperty("type")
    public String getType() {
        return "Connected";
    }

    /**
     * @return The unique identifier of the request
     */
    @JsonProperty("request_id")
    public String getRequestId() {
        return requestId;
    }

    /**
     * @return Starts at <code>0</code> and increments for each message the server sends
     * to the client.  This includes messages of other types, like
     * <code>TurnInfo</code> messages.
     */
    @JsonProperty("sequence_id")
    public double getSequenceId() {
        return sequenceId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ListenV2Connected && equalTo((ListenV2Connected) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ListenV2Connected other) {
        return requestId.equals(other.requestId) && sequenceId == other.sequenceId;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.requestId, this.sequenceId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static RequestIdStage builder() {
        return new Builder();
    }

    public interface RequestIdStage {
        /**
         * <p>The unique identifier of the request</p>
         */
        SequenceIdStage requestId(@NotNull String requestId);

        Builder from(ListenV2Connected other);
    }

    public interface SequenceIdStage {
        /**
         * <p>Starts at <code>0</code> and increments for each message the server sends
         * to the client.  This includes messages of other types, like
         * <code>TurnInfo</code> messages.</p>
         */
        _FinalStage sequenceId(double sequenceId);
    }

    public interface _FinalStage {
        ListenV2Connected build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements RequestIdStage, SequenceIdStage, _FinalStage {
        private String requestId;

        private double sequenceId;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ListenV2Connected other) {
            requestId(other.getRequestId());
            sequenceId(other.getSequenceId());
            return this;
        }

        /**
         * <p>The unique identifier of the request</p>
         * <p>The unique identifier of the request</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("request_id")
        public SequenceIdStage requestId(@NotNull String requestId) {
            this.requestId = Objects.requireNonNull(requestId, "requestId must not be null");
            return this;
        }

        /**
         * <p>Starts at <code>0</code> and increments for each message the server sends
         * to the client.  This includes messages of other types, like
         * <code>TurnInfo</code> messages.</p>
         * <p>Starts at <code>0</code> and increments for each message the server sends
         * to the client.  This includes messages of other types, like
         * <code>TurnInfo</code> messages.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("sequence_id")
        public _FinalStage sequenceId(double sequenceId) {
            this.sequenceId = sequenceId;
            return this;
        }

        @java.lang.Override
        public ListenV2Connected build() {
            return new ListenV2Connected(requestId, sequenceId, additionalProperties);
        }
    }
}
