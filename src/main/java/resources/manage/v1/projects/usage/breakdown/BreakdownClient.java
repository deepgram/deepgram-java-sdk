/** This file was auto-generated by Fern from our API Definition. */
package resources.manage.v1.projects.usage.breakdown;

import core.ClientOptions;
import core.RequestOptions;
import resources.manage.v1.projects.usage.breakdown.requests.BreakdownGetRequest;
import types.UsageBreakdownV1Response;

public class BreakdownClient {
  protected final ClientOptions clientOptions;

  private final RawBreakdownClient rawClient;

  public BreakdownClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawBreakdownClient(clientOptions);
  }

  /** Get responses with HTTP metadata like headers */
  public RawBreakdownClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Retrieves the usage breakdown for a specific project, with various filter options by API
   * feature or by groupings. Setting a feature (e.g. diarize) to true includes requests that used
   * that feature, while false excludes requests that used it. Multiple true filters are combined
   * with OR logic, while false filters use AND logic.
   */
  public UsageBreakdownV1Response get(String projectId) {
    return this.rawClient.get(projectId).body();
  }

  /**
   * Retrieves the usage breakdown for a specific project, with various filter options by API
   * feature or by groupings. Setting a feature (e.g. diarize) to true includes requests that used
   * that feature, while false excludes requests that used it. Multiple true filters are combined
   * with OR logic, while false filters use AND logic.
   */
  public UsageBreakdownV1Response get(String projectId, RequestOptions requestOptions) {
    return this.rawClient.get(projectId, requestOptions).body();
  }

  /**
   * Retrieves the usage breakdown for a specific project, with various filter options by API
   * feature or by groupings. Setting a feature (e.g. diarize) to true includes requests that used
   * that feature, while false excludes requests that used it. Multiple true filters are combined
   * with OR logic, while false filters use AND logic.
   */
  public UsageBreakdownV1Response get(String projectId, BreakdownGetRequest request) {
    return this.rawClient.get(projectId, request).body();
  }

  /**
   * Retrieves the usage breakdown for a specific project, with various filter options by API
   * feature or by groupings. Setting a feature (e.g. diarize) to true includes requests that used
   * that feature, while false excludes requests that used it. Multiple true filters are combined
   * with OR logic, while false filters use AND logic.
   */
  public UsageBreakdownV1Response get(
      String projectId, BreakdownGetRequest request, RequestOptions requestOptions) {
    return this.rawClient.get(projectId, request, requestOptions).body();
  }
}
