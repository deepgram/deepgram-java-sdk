/** This file was auto-generated by Fern from our API Definition. */
package resources.manage.v1.projects.usage.fields;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.DeepgramApiApiException;
import core.DeepgramApiException;
import core.DeepgramApiHttpResponse;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import errors.BadRequestError;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.manage.v1.projects.usage.fields.requests.FieldsListRequest;
import types.UsageFieldsV1Response;

public class RawFieldsClient {
  protected final ClientOptions clientOptions;

  public RawFieldsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Lists the features, models, tags, languages, and processing method used for requests in the
   * specified project
   */
  public DeepgramApiHttpResponse<UsageFieldsV1Response> list(String projectId) {
    return list(projectId, FieldsListRequest.builder().build());
  }

  /**
   * Lists the features, models, tags, languages, and processing method used for requests in the
   * specified project
   */
  public DeepgramApiHttpResponse<UsageFieldsV1Response> list(
      String projectId, RequestOptions requestOptions) {
    return list(projectId, FieldsListRequest.builder().build(), requestOptions);
  }

  /**
   * Lists the features, models, tags, languages, and processing method used for requests in the
   * specified project
   */
  public DeepgramApiHttpResponse<UsageFieldsV1Response> list(
      String projectId, FieldsListRequest request) {
    return list(projectId, request, null);
  }

  /**
   * Lists the features, models, tags, languages, and processing method used for requests in the
   * specified project
   */
  public DeepgramApiHttpResponse<UsageFieldsV1Response> list(
      String projectId, FieldsListRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl =
        HttpUrl.parse(this.clientOptions.environment().getBaseURL())
            .newBuilder()
            .addPathSegments("v1/projects")
            .addPathSegment(projectId)
            .addPathSegments("usage")
            .addPathSegments("fields");
    if (request.getStart().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "start", request.getStart().get(), false);
    }
    if (request.getEnd().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "end", request.getEnd().get(), false);
    }
    if (requestOptions != null) {
      requestOptions
          .getQueryParameters()
          .forEach(
              (_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              });
    }
    Request.Builder _requestBuilder =
        new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
    Request okhttpRequest = _requestBuilder.build();
    OkHttpClient client = clientOptions.httpClient();
    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
      client = clientOptions.httpClientWithTimeout(requestOptions);
    }
    try (Response response = client.newCall(okhttpRequest).execute()) {
      ResponseBody responseBody = response.body();
      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
      if (response.isSuccessful()) {
        return new DeepgramApiHttpResponse<>(
            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UsageFieldsV1Response.class),
            response);
      }
      try {
        if (response.code() == 400) {
          throw new BadRequestError(
              ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
        }
      } catch (JsonProcessingException ignored) {
        // unable to map error response, throwing generic error
      }
      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
      throw new DeepgramApiApiException(
          "Error with status code " + response.code(), response.code(), errorBody, response);
    } catch (IOException e) {
      throw new DeepgramApiException("Network error executing HTTP request", e);
    }
  }
}
