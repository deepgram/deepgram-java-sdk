/** This file was auto-generated by Fern from our API Definition. */
package resources.read.v1.text;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.DeepgramApiApiException;
import core.DeepgramApiException;
import core.DeepgramApiHttpResponse;
import core.MediaTypes;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import errors.BadRequestError;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.read.v1.text.requests.TextAnalyzeRequest;
import types.ReadV1Request;
import types.ReadV1Response;

public class AsyncRawTextClient {
  protected final ClientOptions clientOptions;

  public AsyncRawTextClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /** Analyze text content using Deepgrams text analysis API */
  public CompletableFuture<DeepgramApiHttpResponse<ReadV1Response>> analyze(ReadV1Request body) {
    return analyze(TextAnalyzeRequest.builder().body(body).build());
  }

  /** Analyze text content using Deepgrams text analysis API */
  public CompletableFuture<DeepgramApiHttpResponse<ReadV1Response>> analyze(
      ReadV1Request body, RequestOptions requestOptions) {
    return analyze(TextAnalyzeRequest.builder().body(body).build(), requestOptions);
  }

  /** Analyze text content using Deepgrams text analysis API */
  public CompletableFuture<DeepgramApiHttpResponse<ReadV1Response>> analyze(
      TextAnalyzeRequest request) {
    return analyze(request, null);
  }

  /** Analyze text content using Deepgrams text analysis API */
  public CompletableFuture<DeepgramApiHttpResponse<ReadV1Response>> analyze(
      TextAnalyzeRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl =
        HttpUrl.parse(this.clientOptions.environment().getBaseURL())
            .newBuilder()
            .addPathSegments("v1/read");
    if (request.getCallback().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "callback", request.getCallback().get(), false);
    }
    if (request.getCallbackMethod().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "callback_method", request.getCallbackMethod().get(), false);
    }
    if (request.getSentiment().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "sentiment", request.getSentiment().get(), false);
    }
    if (request.getSummarize().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "summarize", request.getSummarize().get(), false);
    }
    if (request.getTopics().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "topics", request.getTopics().get(), false);
    }
    if (request.getCustomTopicMode().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "custom_topic_mode", request.getCustomTopicMode().get(), false);
    }
    if (request.getIntents().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "intents", request.getIntents().get(), false);
    }
    if (request.getCustomIntentMode().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "custom_intent_mode", request.getCustomIntentMode().get(), false);
    }
    if (request.getLanguage().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "language", request.getLanguage().get(), false);
    }
    if (request.getTag().isPresent()) {
      QueryStringMapper.addQueryParameter(httpUrl, "tag", request.getTag().get(), true);
    }
    if (request.getCustomTopic().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "custom_topic", request.getCustomTopic().get(), true);
    }
    if (request.getCustomIntent().isPresent()) {
      QueryStringMapper.addQueryParameter(
          httpUrl, "custom_intent", request.getCustomIntent().get(), true);
    }
    if (requestOptions != null) {
      requestOptions
          .getQueryParameters()
          .forEach(
              (_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              });
    }
    RequestBody body;
    try {
      body =
          RequestBody.create(
              ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()),
              MediaTypes.APPLICATION_JSON);
    } catch (Exception e) {
      throw new RuntimeException(e);
    }
    Request.Builder _requestBuilder =
        new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json");
    Request okhttpRequest = _requestBuilder.build();
    OkHttpClient client = clientOptions.httpClient();
    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
      client = clientOptions.httpClientWithTimeout(requestOptions);
    }
    CompletableFuture<DeepgramApiHttpResponse<ReadV1Response>> future = new CompletableFuture<>();
    client
        .newCall(okhttpRequest)
        .enqueue(
            new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response)
                  throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(
                        new DeepgramApiHttpResponse<>(
                            ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ReadV1Response.class),
                            response));
                    return;
                  }
                  try {
                    if (response.code() == 400) {
                      future.completeExceptionally(
                          new BadRequestError(
                              ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                              response));
                      return;
                    }
                  } catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(
                      new DeepgramApiApiException(
                          "Error with status code " + response.code(),
                          response.code(),
                          errorBody,
                          response));
                  return;
                } catch (IOException e) {
                  future.completeExceptionally(
                      new DeepgramApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                    new DeepgramApiException("Network error executing HTTP request", e));
              }
            });
    return future;
  }
}
