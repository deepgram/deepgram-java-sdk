/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.agent.v1.settings.think.models;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.DeepgramApiApiException;
import core.DeepgramApiException;
import core.DeepgramApiHttpResponse;
import core.ObjectMappers;
import core.RequestOptions;
import errors.BadRequestError;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import types.AgentThinkModelsV1Response;

public class AsyncRawModelsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawModelsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieves the available think models that can be used for AI agent processing
   */
  public CompletableFuture<DeepgramApiHttpResponse<AgentThinkModelsV1Response>> list() {
    return list(null);
  }

  /**
   * Retrieves the available think models that can be used for AI agent processing
   */
  public CompletableFuture<DeepgramApiHttpResponse<AgentThinkModelsV1Response>> list(
      RequestOptions requestOptions) {
    HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getBaseURL()).newBuilder()

      .addPathSegments("v1/agent/settings/think/models")
      .build();
    Request okhttpRequest = new Request.Builder()
      .url(httpUrl)
      .method("GET", null)
      .headers(Headers.of(clientOptions.headers(requestOptions)))
      .addHeader("Accept", "application/json")
      .build();
    OkHttpClient client = clientOptions.httpClient();
    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
      client = clientOptions.httpClientWithTimeout(requestOptions);
    }
    CompletableFuture<DeepgramApiHttpResponse<AgentThinkModelsV1Response>> future = new CompletableFuture<>();
    client.newCall(okhttpRequest).enqueue(new Callback() {
      @Override
      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
        try (ResponseBody responseBody = response.body()) {
          if (response.isSuccessful()) {
            future.complete(new DeepgramApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), AgentThinkModelsV1Response.class), response));
            return;
          }
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          try {
            if (response.code() == 400) {
              future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
              return;
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          future.completeExceptionally(new DeepgramApiApiException("Error with status code " + response.code(), response.code(), ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
          return;
        }
        catch (IOException e) {
          future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
        }
      }

      @Override
      public void onFailure(@NotNull Call call, @NotNull IOException e) {
        future.completeExceptionally(new DeepgramApiException("Network error executing HTTP request", e));
      }
    });
    return future;
  }
}
